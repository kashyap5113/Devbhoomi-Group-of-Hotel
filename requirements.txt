# Django Hotel Booking System - Python Dependencies
# File Location: requirements.txt (in project root)
#
# Install all dependencies with:
# pip install -r requirements.txt

# Django Framework
Django==4.2.7

# Image Processing (for hotel images, profile pictures)
Pillow==10.1.0

# Optional but recommended packages:

# For better admin interface
django-admin-interface==0.26.1

# For form styling
django-crispy-forms==2.1
crispy-bootstrap5==0.7

# For environment variables management
python-decouple==3.8

# For timezone support
pytz==2023.3

# For database migrations
django-extensions==3.2.3

# For API (if needed in future)
djangorestframework==3.14.0

# For CORS headers (if building API)
django-cors-headers==4.3.1

# For production deployment
gunicorn==21.2.0
whitenoise==6.6.0

# For PostgreSQL (if switching from SQLite)
# psycopg2-binary==2.9.9

# For MySQL (if switching from SQLite)
# mysqlclient==2.2.0